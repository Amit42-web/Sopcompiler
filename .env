# Committed defaults (non-secret). Put secrets in .env.local (git-ignored).

# Zero-config database: a local SQLite file. The app runs and persists with
# no database server. For production/scale, override DATABASE_URL in .env.local
# with a PostgreSQL URL and set the datasource provider to "postgresql".
DATABASE_URL="file:./dev.db"
